{
  "name": "@entropy/core",
  "version": "0.0.1",
  "description": "Core package for EntropyJs",
  "main": "index.js",
  "scripts": {
    "test": "mocha -t 100000 -r ts-node/register 'src/tests.test.ts'",
    "clean": "rm -Rf node_modules && yarn cache clean --all && yarn install"
  },
  "keywords": [],
  "author": "Entropy",
  "license": "ISC",
  "dependencies": {
    "@entropy/substrate": "0.0.1",
    "ethers": "^5.7.2"
  }
}
